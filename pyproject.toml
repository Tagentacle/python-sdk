[project]
name = "tagentacle-py"
version = "0.1.0"
description = "Tagentacle Python SDK — ROS for AI Agent"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "mcp>=1.0",
    "anyio>=4.0",
]
